{
  "name": "kicktick",
  "version": "1.0.0",
  "private": true,
  "description": "KickTick: sub-minute micro prediction markets with TxODDS oracle settlement on Solana",
  "scripts": {
    "simulation": "node simulation.js",
    "sim": "node simulation.js",
    "setup": "./setup-local.sh",
    "frontend": "cd frontend && npm run dev",
    "frontend:build": "cd frontend && npm run build",
    "relayer": "cd relayer && npm start",
    "build:contracts": "./scripts/build.sh contracts dev",
    "build:all": "./scripts/build.sh all dev",
    "deploy:devnet": "./scripts/deploy.sh devnet"
  },
  "keywords": [
    "solana",
    "anchor",
    "prediction-markets",
    "txodds",
    "world-cup",
    "hackathon"
  ],
  "license": "MIT",
  "dependencies": {
    "@solana/spl-token": "^0.4.14"
  },
  "devDependencies": {
    "@types/bs58": "^4.0.4"
  }
}
